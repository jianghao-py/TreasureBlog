### 溢出

块级元素

- 如果父元素没有设置高度，默认下父元素的高度被内容（子元素）撑开
- 如果父元素没有设置宽度，默认下父元素的宽度是祖父元素的全部，是祖父元素宽度的100%

<br>

子元素高度超过父元素,会溢出。溢出的部分可以用 overflow 处理:

```css
overflow: visible; 默认值
overflow: hidden;
overflow: scroll; 将隐藏所有溢出的内容并使滚动条出现在相关元素上。如果内容没有溢出，滚动条仍然可见，但被禁用。
overflow: auto; 和scroll非常相似，但滚动条仅在内容溢出时出现
```